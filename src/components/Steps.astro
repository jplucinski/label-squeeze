---
const steps = [
  {
    title: 'Upload',
    description: 'Upload your A6 shipping labels in PDF format',
    icon: '📤'
  },
  {
    title: 'Combine',
    description: 'We\'ll automatically combine them into A4 pages',
    icon: '🔄'
  },
  {
    title: 'Download',
    description: 'Download your optimized PDF ready for printing',
    icon: '📥'
  }
];
---

<section class="py-12 bg-gray-50 rounded-lg">
  <div class="container mx-auto px-4">
    <h2 class="text-2xl font-bold text-center mb-8">How It Works</h2>
    <div class="grid md:grid-cols-3 gap-8">
      {steps.map((step, index) => (
        <div class="text-center">
          <div class="text-4xl mb-4">{step.icon}</div>
          <h3 class="text-xl font-semibold mb-2">
            {index + 1}. {step.title}
          </h3>
          <p class="text-gray-600">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section> 